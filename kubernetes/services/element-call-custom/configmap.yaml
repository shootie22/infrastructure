apiVersion: v1
kind: ConfigMap
metadata:
  name: element-call-config
  namespace: element-call
data:
  config.json: |
    {
      "matrix_rtc_mode": "compatibility",
      "livekit": {
        "livekit_service_url": "https://a.nuke.zip/livekit/jwt"
      },
      "default_server_config": {
        "m.homeserver": {
          "base_url": "https://nuke.zip",
          "server_name": "nuke.zip"
        }
      }
    }
