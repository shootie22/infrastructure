apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: legacy-api-data
  namespace: legacy-web
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 3Gi
