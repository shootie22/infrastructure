apiVersion: v1
kind: Service
metadata:
  name: legacy-api
  namespace: legacy-web
spec:
  selector:
    app: legacy-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
