version: "3.9"

services:
  web:
    image: nginx:stable-alpine
    container_name: tiny_web
    ports:
      - "${NGINX_TEST_HOST_PORT}:80"
#    volumes:
#      - ~/services/nginx-test/html:/usr/share/nginx/html:ro
#      - ~/services/nginx-test/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
