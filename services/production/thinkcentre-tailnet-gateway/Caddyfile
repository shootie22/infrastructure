{
    auto_https off
    admin off
}

:8080 {
    @health path /__gateway_health
    handle @health {
        respond "ok" 200
    }

    @analytics host analytics.radunenu.com
    handle @analytics {
        reverse_proxy rybbit-caddy:8080
    }

    handle {
        respond "unknown host" 404
    }
}
