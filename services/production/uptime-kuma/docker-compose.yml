services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped
    volumes:
      - /home/ubuntu/services/uptime-kuma:/app/data
    ports:
      # <Host Port>:<Container Port>
      - "127.0.0.1:3039:3001"
