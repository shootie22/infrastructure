services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped
    volumes:
      - ${UPTIME_KUMA_ROOT}:/app/data
    ports:
      # <Host Port>:<Container Port>
      - "127.0.0.1:${UPTIME_KUMA_HOST_PORT}:3001"
