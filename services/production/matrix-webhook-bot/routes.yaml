routes:
  - name: grafana
    path: /hooks/grafana
    room_id: "!replace-me:example.com"
    messages:
      - |
        Grafana alert: {{ .body.status }}
      - |
        {{ .body.title }}
      - |
        {{ .body.message }}
